# function to open the file fileName and producing summaries of the data in the file
def readFile(fileName):
    print(fileName)
    f = open(fileName, "r")
    # TODO: create a script for the user to interact with the summary of the file